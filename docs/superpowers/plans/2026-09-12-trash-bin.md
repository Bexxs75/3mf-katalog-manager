# Papierkorb (Soft-Delete) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gelöschte Modelle landen in einem Papierkorb statt sofort und unwiderruflich entfernt zu werden — die echte Datei wird in ein App-eigenes Verzeichnis verschoben, der Katalog-Eintrag bleibt 7 Tage als "gelöscht" markiert erhalten, danach automatisches endgültiges Entfernen.

**Architecture:** Zwei neue nullable Spalten (`deleted_at`, `trash_path`) auf `files`. "Löschen" wird von "Datei entfernen + DB-Zeile löschen" zu "Datei in `app_data_dir/trash/` verschieben + Spalten setzen" umgebaut. Alle bestehenden Lese-Abfragen gegen `files` bekommen einen `deleted_at IS NULL`-Filter, damit gelöschte Einträge aus der normalen Katalogansicht verschwinden. Eine neue Papierkorb-Ansicht (Header-Icon) zeigt nur die gefilterten Gegenteil-Einträge, mit Wiederherstellen/Endgültig-löschen statt der normalen Aktionen. Kein Hintergrund-Timer — Aufräumen läuft einmalig beim App-Start, analog zum bestehenden `content_hash`-Backfill.

**Tech Stack:** Rust (Tauri 2, rusqlite, chrono), React 19 + TypeScript, Tailwind.

## Global Constraints

- Backend-Tests: `cd src-tauri && cargo test` — muss nach jedem Backend-Task grün bleiben (Stand vor diesem Plan: 84/84, siehe vorheriger Plan zur Modell-Detailseite).
- Frontend: `npx tsc --noEmit` (Projekt-Root) nach jedem Frontend-Task fehlerfrei.
- `Translations`-Interface erzwingt Vollständigkeit — jeder neue i18n-Key MUSS in allen 4 Sprachdateien (de/en/es/fr) UND in `types.ts` ergänzt werden.
- Migrations-Muster: `ALTER TABLE files ADD COLUMN ...` als fehlertolerante Einzelzeile (`let _ = conn.execute(...)`) in `repository.rs`s `init()`, zusätzlich Teil von `CREATE TABLE IF NOT EXISTS files` in `schema.sql`.
- `get_file` (Einzel-Lookup per ID) bekommt **keinen** `deleted_at`-Filter — wird auch für Papierkorb-Einträge gebraucht (Restore/Endgültig-löschen). Nur Listen-Abfragen (`list_files`, Zähl-Abfragen, Existenz-Checks) werden gefiltert.
- `insert_file`/`NewFile` bleiben unverändert — `deleted_at`/`trash_path` werden nie beim Import gesetzt, nur später per dediziertem UPDATE.
- Commit-Attribution: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` unter jedem Commit, plus `Claude-Session: https://claude.ai/code/session_01WAQ5i49v1oEqoX5RzCBUz6`.

---

### Task 1: Backend — Schema, `AppState.trash_dir`, gefilterte Lese-Abfragen, neue Repository-Funktionen

**Files:**
- Modify: `src-tauri/src/db/schema.sql`
- Modify: `src-tauri/src/db/repository.rs` (`init`, `FileRecord`, `row_to_file`, `list_files`, `get_file`, `list_creator_counts`, `list_tag_counts`, `file_exists_by_path`, `file_exists_by_hash`, `max_queue_position`, `list_files_missing_content_hash`, neue Funktionen)
- Modify: `src-tauri/src/commands.rs` (`AppState`, `ModelFileDto`, `to_dto`)
- Test: `src-tauri/src/db/mod.rs`

**Interfaces:**
- Produces:
  - `pub fn soft_delete_file(conn: &Connection, id: i64, trash_path: &str, deleted_at: &str) -> Result<(), DbError>`
  - `pub fn restore_file(conn: &Connection, id: i64, new_path: Option<&str>) -> Result<(), DbError>` (`new_path: None` = Pfad unverändert, `Some(p)` = Pfad wurde durch Konfliktauflösung geändert)
  - `pub fn list_trash(conn: &Connection) -> Result<Vec<FileRecord>, DbError>`
  - `pub fn purge_expired_trash(conn: &Connection, older_than: &str) -> Result<Vec<FileRecord>, DbError>` (gibt betroffene Zeilen zurück, löscht sie NICHT selbst — Aufrufer entfernt zuerst die echten Dateien, dann per bestehendem `delete_file` die DB-Zeilen)
  - `FileRecord.deleted_at: Option<String>`, `FileRecord.trash_path: Option<String>`
  - `AppState.trash_dir: std::path::PathBuf` — für Task 2.
  - `ModelFileDto.deleted_at: Option<String>` — für Task 3/4 (Frontend zeigt "wird entfernt am +7 Tage").

- [ ] **Step 1: Schema-Migration**

In `src-tauri/src/db/schema.sql`, im `CREATE TABLE IF NOT EXISTS files (...)`-Block zwei Zeilen ergänzen (neben `plate_count INTEGER,`):

```sql
    deleted_at TEXT,
    trash_path TEXT,
```

In `src-tauri/src/db/repository.rs`s `init()`, neben der bestehenden `plate_count`-Migrationszeile:

```rust
    let _ = conn.execute("ALTER TABLE files ADD COLUMN deleted_at TEXT", []);
    let _ = conn.execute("ALTER TABLE files ADD COLUMN trash_path TEXT", []);
```

- [ ] **Step 2: `FileRecord` erweitern, `row_to_file`/SELECT-Listen anpassen**

`FileRecord`-Struct (`db/models.rs`) um zwei Felder ergänzen (nach `plate_count`, falls dort vorhanden — sonst ans Ende):

```rust
    pub deleted_at: Option<String>,
    pub trash_path: Option<String>,
```

In `list_files` und `get_file`s SELECT-Statements (`repository.rs`): `deleted_at, trash_path` ans Ende der Spaltenliste anhängen. In `row_to_file`: `deleted_at: row.get(N)?, trash_path: row.get(N+1)?,` mit `N` = nächster freier Index nach der zuletzt vorhandenen Spalte (per tatsächlicher Spaltenanzahl in der SELECT-Liste zum Zeitpunkt der Umsetzung zählen, nicht blind einen Indexwert übernehmen — die exakte Zahl hängt davon ab, ob `plate_count` bereits als letzte Spalte vorhanden ist).

**`list_files` bekommt zusätzlich einen Filter**: `WHERE deleted_at IS NULL` vor `ORDER BY name` ergänzen. **`get_file` bekommt KEINEN Filter** (siehe Global Constraints).

- [ ] **Step 3: Übrige Lese-Abfragen filtern**

`list_creator_counts`:
```rust
        "SELECT creator, COUNT(*) FROM files WHERE creator IS NOT NULL AND deleted_at IS NULL GROUP BY creator ORDER BY creator",
```

`list_tag_counts` — die Zählung muss Tags von Papierkorb-Dateien ausschließen, aber Tags mit 0 aktiven Dateien weiterhin anzeigen (bestehendes `LEFT JOIN`-Verhalten). Ersetze:
```rust
        "SELECT t.name, t.color_hue, COUNT(ft.file_id)
         FROM tags t
         LEFT JOIN file_tags ft ON ft.tag_id = t.id
         GROUP BY t.id
         ORDER BY t.name",
```
durch:
```rust
        "SELECT t.name, t.color_hue, COUNT(f.id)
         FROM tags t
         LEFT JOIN file_tags ft ON ft.tag_id = t.id
         LEFT JOIN files f ON f.id = ft.file_id AND f.deleted_at IS NULL
         GROUP BY t.id
         ORDER BY t.name",
```

`file_exists_by_path`:
```rust
        .query_row("SELECT 1 FROM files WHERE path = ?1 AND deleted_at IS NULL", params![path], |row| {
```

`file_exists_by_hash`:
```rust
            "SELECT 1 FROM files WHERE content_hash = ?1 AND deleted_at IS NULL",
```

`max_queue_position`:
```rust
    Ok(conn.query_row("SELECT MAX(queue_position) FROM files WHERE deleted_at IS NULL", [], |row| row.get(0))?)
```

`list_files_missing_content_hash`:
```rust
    let mut stmt = conn.prepare("SELECT id, path FROM files WHERE content_hash IS NULL AND deleted_at IS NULL")?;
```

Diese beiden letzten Änderungen sind wichtig, nicht kosmetisch: ohne den Filter würde `file_exists_by_path`/`file_exists_by_hash` einen neuen Import fälschlich als Duplikat einer im Papierkorb liegenden Datei ablehnen (deren `path`/`content_hash`-Werte in der DB erhalten bleiben, obwohl die Datei physisch verschoben wurde).

- [ ] **Step 4: Neue Repository-Funktionen**

In `repository.rs`, neben `delete_file`:

```rust
pub fn soft_delete_file(conn: &Connection, id: i64, trash_path: &str, deleted_at: &str) -> Result<(), DbError> {
    conn.execute(
        "UPDATE files SET deleted_at = ?1, trash_path = ?2 WHERE id = ?3",
        params![deleted_at, trash_path, id],
    )?;
    Ok(())
}

pub fn restore_file(conn: &Connection, id: i64, new_path: Option<&str>) -> Result<(), DbError> {
    match new_path {
        Some(path) => {
            conn.execute(
                "UPDATE files SET deleted_at = NULL, trash_path = NULL, path = ?1 WHERE id = ?2",
                params![path, id],
            )?;
        }
        None => {
            conn.execute(
                "UPDATE files SET deleted_at = NULL, trash_path = NULL WHERE id = ?1",
                params![id],
            )?;
        }
    }
    Ok(())
}

const TRASH_SELECT_COLUMNS: &str = "id, name, path, file_type, folder_id, origin, sync_status, cloud_id,
     file_size_bytes, dimension_x_mm, dimension_y_mm, dimension_z_mm,
     volume_cm3, object_count, thumbnail_png, imported_at, file_modified_at,
     print_status, last_viewed_at, creator, content_hash,
     render_snapshot_png, custom_image_png, source_url, queue_position, favorite,
     plate_count, deleted_at, trash_path";

pub fn list_trash(conn: &Connection) -> Result<Vec<FileRecord>, DbError> {
    let sql = format!("SELECT {TRASH_SELECT_COLUMNS} FROM files WHERE deleted_at IS NOT NULL ORDER BY deleted_at DESC");
    let mut stmt = conn.prepare(&sql)?;
    let mut files = stmt.query_map([], row_to_file)?.collect::<Result<Vec<_>, _>>()?;
    for file in &mut files {
        file.materials = load_materials(conn, file.id)?;
        file.metadata = load_metadata(conn, file.id)?;
        file.tags = load_tags(conn, file.id)?;
    }
    Ok(files)
}

pub fn purge_expired_trash(conn: &Connection, older_than: &str) -> Result<Vec<FileRecord>, DbError> {
    let sql = format!("SELECT {TRASH_SELECT_COLUMNS} FROM files WHERE deleted_at IS NOT NULL AND deleted_at < ?1");
    let mut stmt = conn.prepare(&sql)?;
    let files = stmt.query_map(params![older_than], row_to_file)?.collect::<Result<Vec<_>, _>>()?;
    Ok(files)
}
```

`TRASH_SELECT_COLUMNS` MUSS exakt dieselbe Spaltenreihenfolge wie `list_files`/`get_file` in Step 2 verwenden, damit `row_to_file` (das per Index liest) korrekt funktioniert — bei Abweichung in Step 2 hier entsprechend nachziehen.

- [ ] **Step 5: `AppState.trash_dir` + `ModelFileDto.deleted_at`**

`src-tauri/src/commands.rs`, `AppState`-Struct:

```rust
pub struct AppState {
    pub db: Mutex<Connection>,
    pub trash_dir: std::path::PathBuf,
}
```

`ModelFileDto`-Struct, neues Feld (nach `plate_count`, falls vorhanden — sonst ans Ende):

```rust
    pub deleted_at: Option<String>,
```

`to_dto`, im Konstruktor: `deleted_at: file.deleted_at,`.

- [ ] **Step 6: Kompilieren, bestehende Tests anpassen, neuer Roundtrip-Test**

`AppState { db: ... }`-Struct-Literale im Testcode (per Grep `AppState {` in `commands.rs`/Tests suchen) brauchen jetzt zusätzlich `trash_dir: std::env::temp_dir(),` (oder einen anderen temporären Pfad — nur wichtig, dass er kompiliert, da diese Tests i. d. R. keine echten Dateien anfassen).

In `src-tauri/src/db/mod.rs`, neuer Test analog zu bestehenden Roundtrip-Tests:

```rust
    #[test]
    fn soft_delete_and_restore_file_roundtrips() {
        let mut conn = test_connection();
        let file = sample_file();
        let id = repository::insert_file(&mut conn, &file).unwrap();

        repository::soft_delete_file(&conn, id, "/trash/1-test.3mf", "2026-01-01T00:00:00Z").unwrap();
        let fetched = repository::get_file(&conn, id).unwrap().unwrap();
        assert_eq!(fetched.deleted_at.as_deref(), Some("2026-01-01T00:00:00Z"));
        assert_eq!(fetched.trash_path.as_deref(), Some("/trash/1-test.3mf"));

        let visible = repository::list_files(&conn).unwrap();
        assert!(visible.iter().all(|f| f.id != id), "geloeschte Datei darf nicht in list_files erscheinen");

        let trashed = repository::list_trash(&conn).unwrap();
        assert!(trashed.iter().any(|f| f.id == id), "geloeschte Datei muss in list_trash erscheinen");

        repository::restore_file(&conn, id, None).unwrap();
        let restored = repository::get_file(&conn, id).unwrap().unwrap();
        assert!(restored.deleted_at.is_none());
        assert!(restored.trash_path.is_none());
    }
```

(`sample_file()`/`test_connection()` sind bestehende Test-Helfer in dieser Datei — Namen ggf. an die tatsächlichen Namen im Code anpassen, siehe Hinweis aus dem vorherigen Plan: hießen zuletzt `sample_file()`/`connect_in_memory()`.)

Run: `cd src-tauri && cargo build && cargo test`
Expected: alle Tests grün (84 vorherige + 1 neuer Test = 85).

- [ ] **Step 7: Commit**

```bash
cd /home/andreasm/Projekte/3mf-katalog-manager
git add src-tauri/src/db/schema.sql src-tauri/src/db/repository.rs src-tauri/src/db/models.rs \
  src-tauri/src/db/mod.rs src-tauri/src/commands.rs
git commit -m "$(cat <<'EOF'
Papierkorb: DB-Schema, gefilterte Abfragen, Repository-Funktionen

Neue nullable Spalten deleted_at/trash_path auf files (fehlertolerante
Migration für Bestands-DBs). Alle bestehenden Lese-Abfragen gegen
files (list_files, Creator-/Tag-Zählungen, Pfad-/Hash-Existenzchecks,
Queue-Position-Max, content_hash-Backfill-Kandidaten) filtern jetzt
deleted_at IS NULL - verhindert u.a., dass ein Import fälschlich als
Duplikat einer im Papierkorb liegenden Datei erkannt wird. get_file
bleibt ungefiltert (wird auch für Papierkorb-Einträge gebraucht). Neue
Repository-Funktionen soft_delete_file/restore_file/list_trash/
purge_expired_trash, noch nicht ans Frontend angebunden (folgt in
Task 2/3).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WAQ5i49v1oEqoX5RzCBUz6
EOF
)"
```

---

### Task 2: Backend — Löschen verschiebt statt entfernt, neue Tauri-Commands, Startup-Aufräumen

**Files:**
- Modify: `src-tauri/src/commands.rs` (`delete_file`, `delete_files`, neue Commands)
- Modify: `src-tauri/src/lib.rs` (`AppState`-Konstruktion, Trash-Verzeichnis, Startup-Purge, Command-Registrierung)

**Interfaces:**
- Consumes: `db::soft_delete_file`, `db::restore_file`, `db::list_trash`, `db::purge_expired_trash`, `AppState.trash_dir` aus Task 1.
- Produces: Tauri-Commands `list_trash`, `restore_file`, `delete_file_permanently`, `empty_trash` — für Task 3/4 (Frontend-`invoke`-Aufrufe).

- [ ] **Step 1: `delete_file` verschiebt statt zu entfernen**

In `src-tauri/src/commands.rs`, `delete_file` komplett ersetzen durch:

```rust
#[tauri::command]
pub fn delete_file(state: State<AppState>, file_id: String) -> CmdResult<()> {
    let id: i64 = file_id.parse().map_err(|_| "invalid file id".to_string())?;
    let conn = lock_db(&state)?;
    let file = db::get_file(&conn, id)
        .map_err(|e| e.to_string())?
        .ok_or_else(|| "file not found".to_string())?;

    if std::fs::metadata(&file.path).is_err() {
        // Datei existiert schon nicht mehr (z.B. bereinigter verwaister
        // Pfad) - nichts zu verschieben, Katalog-Eintrag direkt hart loeschen.
        return db::delete_file(&conn, id).map_err(|e| e.to_string());
    }

    let trash_path = state.trash_dir.join(format!("{id}-{}", file.name));
    move_file(std::path::Path::new(&file.path), &trash_path).map_err(|e| e.to_string())?;

    let deleted_at = chrono::Utc::now().to_rfc3339();
    db::soft_delete_file(&conn, id, &trash_path.to_string_lossy(), &deleted_at)
        .map_err(|e| e.to_string())
}

/// Verschiebt eine Datei; faellt bei "CrossesDevices" (Ziel auf anderem
/// Dateisystem) auf Kopieren+Loeschen des Originals zurueck.
fn move_file(from: &std::path::Path, to: &std::path::Path) -> std::io::Result<()> {
    match std::fs::rename(from, to) {
        Ok(()) => Ok(()),
        Err(e) if e.kind() == std::io::ErrorKind::CrossesDevices => {
            std::fs::copy(from, to)?;
            std::fs::remove_file(from)
        }
        Err(e) => Err(e),
    }
}
```

- [ ] **Step 2: `delete_files` (Batch, Aufräum-Vorschläge) verschiebt statt zu entfernen**

In `delete_files`, den bestehenden Block

```rust
        if let Err(e) = std::fs::remove_file(&file.path) {
            if e.kind() != std::io::ErrorKind::NotFound {
                eprintln!("[cleanup] Loeschen fehlgeschlagen fuer Datei-ID {id}: {e}");
                continue;
            }
        }
        if let Err(e) = db::delete_file(&conn, id) {
            eprintln!("[cleanup] DB-Eintrag konnte nicht geloescht werden fuer Datei-ID {id}: {e}");
        }
```

ersetzen durch:

```rust
        if std::fs::metadata(&file.path).is_err() {
            if let Err(e) = db::delete_file(&conn, id) {
                eprintln!("[cleanup] DB-Eintrag konnte nicht geloescht werden fuer Datei-ID {id}: {e}");
            }
            continue;
        }
        let trash_path = state.trash_dir.join(format!("{id}-{}", file.name));
        if let Err(e) = move_file(std::path::Path::new(&file.path), &trash_path) {
            eprintln!("[cleanup] Verschieben in Papierkorb fehlgeschlagen fuer Datei-ID {id}: {e}");
            continue;
        }
        let deleted_at = chrono::Utc::now().to_rfc3339();
        if let Err(e) = db::soft_delete_file(&conn, id, &trash_path.to_string_lossy(), &deleted_at) {
            eprintln!("[cleanup] DB-Eintrag konnte nicht als geloescht markiert werden fuer Datei-ID {id}: {e}");
        }
```

- [ ] **Step 3: Neue Commands**

```rust
#[tauri::command]
pub fn list_trash(state: State<AppState>) -> CmdResult<Vec<ModelFileDto>> {
    let conn = lock_db(&state)?;
    let files = db::list_trash(&conn).map_err(|e| e.to_string())?;
    Ok(files.into_iter().map(to_dto).collect())
}

#[tauri::command]
pub fn restore_file(state: State<AppState>, file_id: String) -> CmdResult<()> {
    let id: i64 = file_id.parse().map_err(|_| "invalid file id".to_string())?;
    let conn = lock_db(&state)?;
    let file = db::get_file(&conn, id)
        .map_err(|e| e.to_string())?
        .ok_or_else(|| "file not found".to_string())?;
    let trash_path = file
        .trash_path
        .clone()
        .ok_or_else(|| "file is not in trash".to_string())?;

    let original = std::path::Path::new(&file.path);
    let target_path = if original.exists() {
        let stem = original.file_stem().and_then(|s| s.to_str()).unwrap_or("datei");
        let ext = original.extension().and_then(|s| s.to_str());
        let parent = original.parent().unwrap_or_else(|| std::path::Path::new("."));
        let new_name = match ext {
            Some(ext) => format!("{stem} (wiederhergestellt).{ext}"),
            None => format!("{stem} (wiederhergestellt)"),
        };
        parent.join(new_name)
    } else {
        original.to_path_buf()
    };

    move_file(std::path::Path::new(&trash_path), &target_path).map_err(|e| e.to_string())?;

    let new_path_str = target_path.to_string_lossy().to_string();
    let new_path_arg = if new_path_str == file.path { None } else { Some(new_path_str.as_str()) };
    db::restore_file(&conn, id, new_path_arg).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn delete_file_permanently(state: State<AppState>, file_id: String) -> CmdResult<()> {
    let id: i64 = file_id.parse().map_err(|_| "invalid file id".to_string())?;
    let conn = lock_db(&state)?;
    let file = db::get_file(&conn, id)
        .map_err(|e| e.to_string())?
        .ok_or_else(|| "file not found".to_string())?;
    if let Some(trash_path) = &file.trash_path {
        if let Err(e) = std::fs::remove_file(trash_path) {
            if e.kind() != std::io::ErrorKind::NotFound {
                return Err(e.to_string());
            }
        }
    }
    db::delete_file(&conn, id).map_err(|e| e.to_string())
}

#[tauri::command]
pub fn empty_trash(state: State<AppState>) -> CmdResult<()> {
    let conn = lock_db(&state)?;
    let files = db::list_trash(&conn).map_err(|e| e.to_string())?;
    for file in files {
        if let Some(trash_path) = &file.trash_path {
            if let Err(e) = std::fs::remove_file(trash_path) {
                if e.kind() != std::io::ErrorKind::NotFound {
                    eprintln!("[trash] Entfernen fehlgeschlagen fuer Datei-ID {}: {e}", file.id);
                    continue;
                }
            }
        }
        if let Err(e) = db::delete_file(&conn, file.id) {
            eprintln!("[trash] DB-Eintrag konnte nicht geloescht werden fuer Datei-ID {}: {e}", file.id);
        }
    }
    Ok(())
}

/// Beim App-Start aufgerufen: entfernt alle Papierkorb-Eintraege, die
/// laenger als 7 Tage zurueckliegen, endgueltig. Einzelne fehlschlagende
/// Datei wird geloggt und uebersprungen, bricht den Rest nicht ab -
/// gleiches Muster wie der bestehende content_hash-Backfill.
pub fn purge_expired_trash_on_startup(conn: &Connection) {
    let cutoff = (chrono::Utc::now() - chrono::Duration::days(7)).to_rfc3339();
    let expired = match db::purge_expired_trash(conn, &cutoff) {
        Ok(files) => files,
        Err(e) => {
            eprintln!("[startup] Papierkorb-Aufraeumen: Abfrage fehlgeschlagen: {e}");
            return;
        }
    };
    for file in expired {
        if let Some(trash_path) = &file.trash_path {
            if let Err(e) = std::fs::remove_file(trash_path) {
                if e.kind() != std::io::ErrorKind::NotFound {
                    eprintln!("[startup] Papierkorb-Aufraeumen: Datei fehlgeschlagen fuer ID {}: {e}", file.id);
                    continue;
                }
            }
        }
        if let Err(e) = db::delete_file(conn, file.id) {
            eprintln!("[startup] Papierkorb-Aufraeumen: DB-Eintrag fehlgeschlagen fuer ID {}: {e}", file.id);
        }
    }
}
```

`purge_expired_trash_on_startup` braucht `use rusqlite::Connection;` bzw. den bereits in `commands.rs` vorhandenen Import (per Grep prüfen, `Connection` wird dort schon für `lock_db` gebraucht).

- [ ] **Step 4: `lib.rs` — Trash-Verzeichnis, `AppState`, Startup-Purge, Command-Registrierung**

In `src-tauri/src/lib.rs`s `setup()`-Closure, nach `std::fs::create_dir_all(&app_data_dir)?;`:

```rust
            let trash_dir = app_data_dir.join("trash");
            std::fs::create_dir_all(&trash_dir)?;
```

Nach `commands::backfill_content_hashes(&conn);`:

```rust
            commands::purge_expired_trash_on_startup(&conn);
```

`app.manage(commands::AppState { db: Mutex::new(conn) });` ändern zu:

```rust
            app.manage(commands::AppState {
                db: Mutex::new(conn),
                trash_dir,
            });
```

Im `invoke_handler(tauri::generate_handler![...])`-Makro-Aufruf, neben den bestehenden `commands::delete_file`/`commands::delete_files`-Einträgen, vier neue Zeilen ergänzen:

```rust
            commands::list_trash,
            commands::restore_file,
            commands::delete_file_permanently,
            commands::empty_trash,
```

- [ ] **Step 5: Kompilieren, testen**

Run: `cd src-tauri && cargo build && cargo test`
Expected: alle Tests grün (85, unverändert gegenüber Task 1 — dieser Task fügt keine neuen `#[test]`-Funktionen hinzu, nur Commands/Wiring).

- [ ] **Step 6: Commit**

```bash
cd /home/andreasm/Projekte/3mf-katalog-manager
git add src-tauri/src/commands.rs src-tauri/src/lib.rs
git commit -m "$(cat <<'EOF'
Papierkorb: Löschen verschiebt Dateien statt sie zu entfernen

delete_file/delete_files verschieben die echte Datei jetzt nach
app_data_dir/trash/ statt sie sofort zu entfernen (Fallback auf
Kopieren+Löschen bei Dateisystemgrenzen). Existiert die Datei am
Originalpfad schon nicht mehr, wird wie bisher direkt hart gelöscht -
nichts zu verschieben. Neue Commands list_trash/restore_file/
delete_file_permanently/empty_trash. restore_file löst Namenskonflikte
am Zielpfad automatisch per " (wiederhergestellt)"-Suffix. Startup
räumt Papierkorb-Einträge älter als 7 Tage automatisch endgültig weg,
analog zum bestehenden content_hash-Backfill-Muster.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WAQ5i49v1oEqoX5RzCBUz6
EOF
)"
```

---

### Task 3: Frontend — Typen, Header-Papierkorb-Button, App.tsx-Zustand

**Files:**
- Modify: `src/types/index.ts` (`ModelFile.deletedAt`)
- Modify: `src/components/Header.tsx` (neuer Papierkorb-Button + Badge)
- Modify: `src/App.tsx` (`mainView` erweitert, Trash-State/Handler)
- Modify: `src/i18n/types.ts`, `src/i18n/de.ts`, `src/i18n/en.ts`, `src/i18n/es.ts`, `src/i18n/fr.ts`

**Interfaces:**
- Consumes: Tauri-Commands `list_trash`, `restore_file`, `delete_file_permanently`, `empty_trash` aus Task 2.
- Produces: `App.tsx`-State `trashModels: ModelFile[]`, Handler `restoreModel(id: string)`, `deleteModelPermanently(id: string)`, `emptyTrash()` — für Task 4 (Papierkorb-Ansicht + `DetailPanel`-Trash-Modus).

- [ ] **Step 1: `ModelFile.deletedAt`-Typ**

`src/types/index.ts`, `ModelFile`-Interface: `deletedAt: string | null;` ergänzen.

- [ ] **Step 2: i18n-Keys**

`src/i18n/types.ts`, `Translations`-Interface:

```typescript
  trashHeading: string;
  trashEmptyState: string;
  emptyTrashButton: string;
  emptyTrashConfirmQuestion: string;
  restoreLabel: string;
  deletePermanentlyLabel: string;
  trashExpiryHint: string;
```

`src/i18n/de.ts`:

```typescript
  trashHeading: 'Papierkorb',
  trashEmptyState: 'Papierkorb ist leer.',
  emptyTrashButton: 'Papierkorb leeren',
  emptyTrashConfirmQuestion: 'Papierkorb endgültig leeren?',
  restoreLabel: 'Wiederherstellen',
  deletePermanentlyLabel: 'Endgültig löschen',
  trashExpiryHint: 'Wird automatisch entfernt am {date}',
```

`src/i18n/en.ts`:

```typescript
  trashHeading: 'Trash',
  trashEmptyState: 'Trash is empty.',
  emptyTrashButton: 'Empty trash',
  emptyTrashConfirmQuestion: 'Empty trash permanently?',
  restoreLabel: 'Restore',
  deletePermanentlyLabel: 'Delete permanently',
  trashExpiryHint: 'Automatically removed on {date}',
```

`src/i18n/es.ts`:

```typescript
  trashHeading: 'Papelera',
  trashEmptyState: 'La papelera está vacía.',
  emptyTrashButton: 'Vaciar papelera',
  emptyTrashConfirmQuestion: '¿Vaciar la papelera definitivamente?',
  restoreLabel: 'Restaurar',
  deletePermanentlyLabel: 'Eliminar definitivamente',
  trashExpiryHint: 'Se elimina automáticamente el {date}',
```

`src/i18n/fr.ts`:

```typescript
  trashHeading: 'Corbeille',
  trashEmptyState: 'La corbeille est vide.',
  emptyTrashButton: 'Vider la corbeille',
  emptyTrashConfirmQuestion: 'Vider définitivement la corbeille ?',
  restoreLabel: 'Restaurer',
  deletePermanentlyLabel: 'Supprimer définitivement',
  trashExpiryHint: 'Suppression automatique le {date}',
```

`{date}` wird in Task 4 clientseitig per String-Ersetzung eingesetzt (kein i18n-Interpolationsmechanismus im Projekt vorhanden — per Grep in `src/i18n/format.ts`/anderen Keys mit Platzhaltern prüfen, ob es ein bestehendes Muster gibt, z. B. `formatCount`; falls ja, dessen Muster übernehmen, sonst simples `.replace('{date}', ...)`).

- [ ] **Step 3: `tsc` verifizieren**

Run: `npx tsc --noEmit`
Expected: kompiliert (Keys noch nicht verwendet, aber vollständig).

- [ ] **Step 4: Header-Papierkorb-Button**

`src/components/Header.tsx`, `Props`-Interface erweitern:

```typescript
  mainView: 'catalog' | 'filament' | 'trash';
  onMainViewChange: (view: 'catalog' | 'filament' | 'trash') => void;
  trashCount: number;
```

Neben dem bestehenden Zahnrad-Button (`⚙`, vor dessen `<div className="relative shrink-0">`-Wrapper), neuer Button im selben Stil:

```typescript
      <button
        onClick={() => onMainViewChange(mainView === 'trash' ? 'catalog' : 'trash')}
        title={t('trashHeading')}
        className="relative shrink-0 w-8 h-8 grid place-items-center rounded-[3px] border border-[var(--line)] bg-[var(--panel-2)] text-[var(--ink-2)] text-[15px] cursor-pointer hover:text-[var(--ink)] hover:border-[var(--line-strong)]"
      >
        🗑
        {trashCount > 0 && (
          <span className="absolute -top-1.5 -right-1.5 min-w-[16px] h-4 px-1 rounded-full bg-[var(--accent)] text-[var(--accent-ink)] text-[9.5px] font-bold grid place-items-center font-mono-ui">
            {trashCount}
          </span>
        )}
      </button>
```

- [ ] **Step 5: `App.tsx` — Trash-State und Handler**

Neuer State neben `mainView`:

```typescript
  const [trashModels, setTrashModels] = useState<ModelFile[]>([]);
  const [confirmEmptyTrash, setConfirmEmptyTrash] = useState(false);
```

`mainView`-Typdeklaration erweitern: `useState<'catalog' | 'filament' | 'trash'>('catalog')`.

Neue Funktionen (neben `refreshFolders`/`refreshTags` etc.):

```typescript
  const refreshTrash = () => invoke<ModelFile[]>('list_trash').then(setTrashModels);

  const restoreModel = (id: string) => {
    invoke('restore_file', { fileId: id }).then(() => {
      setTrashModels((prev) => prev.filter((m) => m.id !== id));
      invoke<ModelFile[]>('list_files').then(setModels);
      refreshFolders();
      refreshTags();
      refreshCreators();
    });
  };

  const deleteModelPermanently = (id: string) => {
    invoke('delete_file_permanently', { fileId: id }).then(() => {
      setTrashModels((prev) => prev.filter((m) => m.id !== id));
    });
  };

  const emptyTrash = () => {
    invoke('empty_trash').then(() => {
      setTrashModels([]);
      setConfirmEmptyTrash(false);
    });
  };
```

`refreshTrash()` einmalig beim Öffnen der Papierkorb-Ansicht aufrufen: im bestehenden `mainView`-Umschalt-Handler (dort, wo `<Header ... onMainViewChange={...} />` verdrahtet ist) — wenn der neue Wert `'trash'` ist, zusätzlich `refreshTrash()` aufrufen. Konkret: dort, wo aktuell `onMainViewChange={setMainView}` (oder eine Inline-Funktion) an `<Header>` übergeben wird, durch folgende Version ersetzen:

```typescript
onMainViewChange={(v) => {
  setMainView(v);
  if (v === 'trash') refreshTrash();
}}
```

`deleteModel` (bestehend) braucht keine Änderung — `delete_file` verschiebt jetzt serverseitig in den Papierkorb, das Frontend-Verhalten (Modell aus `models` filtern) bleibt korrekt, da die Datei aus der normalen Ansicht verschwindet. Nach einem `deleteModel`-Aufruf zusätzlich `refreshTrash()` aufrufen, damit der Badge-Zähler im Header sofort aktuell ist, auch wenn die Papierkorb-Ansicht gerade nicht offen ist — in `deleteModel`s `.then(() => { ... })`-Block ergänzen: `refreshTrash();`.

`<Header ... />`-Aufruf um die neue Prop ergänzen: `trashCount={trashModels.length}`.

- [ ] **Step 6: `tsc` verifizieren**

Run: `npx tsc --noEmit`
Expected: kompiliert fehlerfrei.

- [ ] **Step 7: Commit**

```bash
cd /home/andreasm/Projekte/3mf-katalog-manager
git add src/types/index.ts src/components/Header.tsx src/App.tsx \
  src/i18n/types.ts src/i18n/de.ts src/i18n/en.ts src/i18n/es.ts src/i18n/fr.ts
git commit -m "$(cat <<'EOF'
Papierkorb: Header-Button mit Badge, App.tsx-Zustand und Handler

mainView um 'trash' erweitert (kein Router, folgt bestehendem
Zustand-Schalt-Muster). Neuer Papierkorb-Icon-Button im Header mit
Zahl-Badge. restoreModel/deleteModelPermanently/emptyTrash rufen die
neuen Backend-Commands auf. Papierkorb-Inhaltsliste wird beim Öffnen
der Ansicht sowie nach jedem Löschen aktualisiert, damit der
Badge-Zähler aktuell bleibt. Noch keine eigene Papierkorb-Ansicht
(folgt in Task 4).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WAQ5i49v1oEqoX5RzCBUz6
EOF
)"
```

---

### Task 4: Frontend — Papierkorb-Ansicht (Grid/Liste wiederverwendet) + `DetailPanel`-Trash-Modus

**Files:**
- Modify: `src/components/ModelGrid.tsx` (`readOnly`-Prop)
- Modify: `src/components/ModelList.tsx` (`readOnly`-Prop)
- Modify: `src/components/DetailPanel.tsx` (neuer `trashMode`-Zweig)
- Modify: `src/App.tsx` (Papierkorb-Render-Zweig)

**Interfaces:**
- Consumes: `trashModels`, `restoreModel`, `deleteModelPermanently`, `emptyTrash` aus Task 3.

- [ ] **Step 1: `readOnly`-Prop auf `ModelGrid`/`ModelList`**

`src/components/ModelGrid.tsx`, `Props`-Interface: `readOnly?: boolean;` ergänzen (optional, Standard `false`/`undefined` = bestehendes Verhalten unverändert).

In beiden Karten-Renderern: den `onDoubleClick={() => onOpenDetail(m.id)}`-Handler (aus dem vorherigen Plan) bedingt weglassen, wenn `readOnly` gesetzt ist — `onDoubleClick={readOnly ? undefined : () => onOpenDetail(m.id)}`. Den bestehenden Favoriten-Stern-Button (falls in der Karte vorhanden, per Grep `onToggleFavorite` suchen) ebenfalls bedingt ausblenden: `{!readOnly && (<button ... onToggleFavorite ...>)}`.

`src/components/ModelList.tsx` analog: `readOnly?: boolean;` in `Props`, `onDoubleClick` bedingt weglassen.

- [ ] **Step 2: `DetailPanel.tsx` — neuer Trash-Modus als eigener früher Render-Zweig**

Neue optionale Props im `Props`-Interface von `DetailPanel.tsx`:

```typescript
  trashMode?: boolean;
  onRestore?: () => void;
  onDeletePermanently?: () => void;
```

Direkt nach der bestehenden `if (density === 'compact') { ... }`/vor der restlichen Render-Logik der Funktion (am Anfang des Funktionskörpers, nach den frühen `if (!model) return null;`-artigen Guards, falls vorhanden — per Lesen des Funktionsanfangs die exakte Stelle finden): einen neuen, eigenständigen Früh-Return-Zweig einfügen, BEVOR die bestehende `density === 'compact'`-Verzweigung geprüft wird:

```typescript
  if (trashMode) {
    const rows = buildMetaRows(model, t, language);
    const expiryDate = model.deletedAt
      ? new Date(new Date(model.deletedAt).getTime() + 7 * 24 * 60 * 60 * 1000)
      : null;
    return (
      <aside className="flex-none w-[336px] flex flex-col min-h-0 bg-[var(--panel)] border-l border-[var(--line)]">
        <div className="flex-none px-4 pt-3.5 pb-3 border-b border-[var(--line)]">
          <div className="text-[14.5px] font-semibold leading-tight break-words">{model.name}</div>
          <div className="font-mono-ui text-[10.5px] text-[var(--ink-3)] pt-1.5">{model.path}</div>
        </div>

        <div className="relative aspect-[4/3] bg-[var(--plate)] border-b border-[var(--line)] overflow-hidden">
          <ModelViewer fileId={model.id} needsSnapshot={false} onSnapshotCaptured={() => {}} />
        </div>

        <div className="flex-1 overflow-y-auto px-4 pt-3.5 pb-1">
          <div className="font-mono-ui text-[10px] tracking-[0.12em] uppercase text-[var(--ink-3)] pb-2">
            {t('metadataHeading')}
          </div>
          {rows.map((row) => (
            <div key={row.label} className="flex items-baseline gap-3 py-1.5 border-b border-[var(--line)]">
              <span className="flex-none w-[108px] text-[12.5px] text-[var(--ink-2)]">{row.label}</span>
              <span className="flex-1 font-mono-ui text-xs text-right">{row.value}</span>
            </div>
          ))}
        </div>

        <div className="flex-none px-4 py-3 border-t border-[var(--line)] bg-[var(--panel-2)] flex flex-col gap-2">
          {expiryDate && (
            <p className="font-mono-ui text-[10.5px] text-[var(--ink-3)]">
              {t('trashExpiryHint').replace('{date}', formatDate(expiryDate.toISOString(), language))}
            </p>
          )}
          <div className="flex gap-2">
            <button
              onClick={onRestore}
              className="flex-1 h-8 rounded-[3px] border border-[var(--line-strong)] bg-[var(--panel)] text-[var(--ink)] text-[12.5px] font-semibold cursor-pointer hover:border-[var(--accent)] hover:text-[var(--accent)]"
            >
              {t('restoreLabel')}
            </button>
            <button
              onClick={onDeletePermanently}
              className="flex-1 h-8 rounded-[3px] border border-[var(--accent)] bg-[var(--accent)] text-[var(--accent-ink)] text-[12.5px] font-semibold cursor-pointer"
            >
              {t('deletePermanentlyLabel')}
            </button>
          </div>
        </div>
      </aside>
    );
  }
```

`formatDate` ist bereits oben in der Datei aus `../i18n/format` importiert (wird für `metaImported` in `buildMetaRows` gebraucht) — kein neuer Import nötig, nur verifizieren, dass es akzeptiert, was `toISOString()` liefert (gleiches Format wie `imported_at`, das ebenfalls ein ISO-Zeitstempel-String ist).

- [ ] **Step 3: `App.tsx` — Papierkorb-Render-Zweig**

Im Hauptbereich, dort wo aktuell zwischen `detailModel`/Grid-Liste umgeschaltet wird (aus dem vorherigen Plan): eine weitere Bedingung für `mainView === 'trash'` ergänzen — VOR der bestehenden `detailModel`-Prüfung, da die Papierkorb-Ansicht Vorrang hat, wenn sie aktiv ist (die Detailseite öffnet sich in der Papierkorb-Ansicht ohnehin nicht, da `ModelGrid`/`ModelList` dort `readOnly` bekommen). Struktur (im JSX-Rückgabewert, an der Stelle des bestehenden `mainView === 'catalog' ? (...) : (<FilamentView />)`-Zweigs, dritte Alternative ergänzen):

```typescript
        mainView === 'trash' ? (
          <div className="flex flex-1 min-h-0">
            <main className="flex-1 min-w-0 flex flex-col">
              <div className="flex-none flex items-center justify-between px-4 py-3 border-b border-[var(--line)]">
                <h1 className="text-[15px] font-semibold">{t('trashHeading')}</h1>
                {confirmEmptyTrash ? (
                  <div className="flex items-center gap-2">
                    <span className="text-[12.5px] font-medium text-[var(--ink)]">
                      {t('emptyTrashConfirmQuestion')}
                    </span>
                    <button
                      onClick={() => setConfirmEmptyTrash(false)}
                      className="h-8 px-3 rounded-[3px] border border-[var(--line-strong)] bg-[var(--panel)] text-[var(--ink)] text-[12.5px] font-semibold cursor-pointer hover:border-[var(--accent)] hover:text-[var(--accent)]"
                    >
                      {t('cancel')}
                    </button>
                    <button
                      onClick={emptyTrash}
                      className="h-8 px-3 rounded-[3px] border border-[var(--accent)] bg-[var(--accent)] text-[var(--accent-ink)] text-[12.5px] font-semibold cursor-pointer"
                    >
                      {t('emptyTrashButton')}
                    </button>
                  </div>
                ) : (
                  <button
                    onClick={() => setConfirmEmptyTrash(true)}
                    disabled={trashModels.length === 0}
                    className="h-8 px-3 rounded-[3px] border border-[var(--line-strong)] bg-[var(--panel)] text-[var(--ink)] text-[12.5px] font-semibold cursor-pointer disabled:opacity-50 hover:border-[var(--accent)] hover:text-[var(--accent)]"
                  >
                    {t('emptyTrashButton')}
                  </button>
                )}
              </div>
              <div className="flex-1 overflow-y-auto p-4">
                {trashModels.length === 0 ? (
                  <p className="font-mono-ui text-[12.5px] text-[var(--ink-3)]">{t('trashEmptyState')}</p>
                ) : view === 'grid' ? (
                  <ModelGrid
                    models={trashModels}
                    selectedId={selectedId}
                    onSelect={selectModel}
                    onOpenDetail={() => {}}
                    onContextMenu={() => {}}
                    onToggleFavorite={() => {}}
                    readOnly
                  />
                ) : (
                  <ModelList
                    models={trashModels}
                    selectedId={selectedId}
                    onSelect={selectModel}
                    onOpenDetail={() => {}}
                    onContextMenu={() => {}}
                    readOnly
                  />
                )}
              </div>
            </main>
            <DetailPanel
              model={trashModels.find((m) => m.id === selectedId) ?? null}
              trashMode
              onRestore={() => selectedId && restoreModel(selectedId)}
              onDeletePermanently={() => selectedId && deleteModelPermanently(selectedId)}
              onAddTag={() => {}}
              onRemoveTag={() => {}}
              onDelete={() => {}}
              onTogglePrintStatus={() => {}}
              onToggleFavorite={() => {}}
              onToggleQueue={() => {}}
              onUploadImage={() => {}}
              onSnapshotCaptured={() => {}}
              onSetSourceUrl={() => {}}
              onOpenInSlicer={() => {}}
              slicers={[]}
              slicerError={null}
            />
          </div>
        ) : mainView === 'catalog' ? (
          /* bestehender catalog-Zweig unveraendert */
```

Die No-Op-Callbacks (`() => {}`) für die im Trash-Modus ungenutzten `DetailPanel`-Props sind bewusst so — `DetailPanel`s `trashMode`-Zweig aus Step 2 rendert einen komplett eigenen frühen Return, der diese Props gar nicht erst aufruft.

- [ ] **Step 4: `tsc` verifizieren**

Run: `npx tsc --noEmit`
Expected: kompiliert fehlerfrei.

- [ ] **Step 5: Live-Verifikation**

`npm run tauri dev` im Hintergrund starten, kurz auf sauberen Start prüfen (Log-Ausgabe, keine Laufzeitfehler), danach beenden. Ein echter interaktiver Klick-Test ist für einen Agenten nicht möglich — das ist in Ordnung, wird in Task 5 durch einen gezielten Backend-Smoke-Test ergänzt.

- [ ] **Step 6: Commit**

```bash
cd /home/andreasm/Projekte/3mf-katalog-manager
git add src/components/ModelGrid.tsx src/components/ModelList.tsx src/components/DetailPanel.tsx src/App.tsx
git commit -m "$(cat <<'EOF'
Papierkorb-Ansicht: Grid/Liste wiederverwendet, DetailPanel-Trash-Modus

Neue readOnly-Prop auf ModelGrid/ModelList blendet Favorit-Toggle und
Detailseiten-Doppelklick aus. DetailPanel bekommt einen eigenen frühen
Render-Zweig für trashMode (gleiche Metadaten-Zeilen + kleine 3D-
Vorschau, aber Wiederherstellen/Endgültig-löschen statt der normalen
Aktionsleiste, plus Ablauf-Hinweis). App.tsx rendert bei
mainView==='trash' die neue Ansicht statt Katalog/Filament.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01WAQ5i49v1oEqoX5RzCBUz6
EOF
)"
```

---

### Task 5: Voller Verifikationslauf inkl. echtem Lösch-/Wiederherstellungs-Zyklus

**Files:** keine Änderungen, nur Verifikation.

- [ ] **Step 1: Backend voll durchtesten**

Run: `cd src-tauri && cargo build && cargo test`
Expected: alle Tests grün (85, siehe Task 1).

- [ ] **Step 2: Frontend voll bauen**

Run: `cd /home/andreasm/Projekte/3mf-katalog-manager && npm run build`
Expected: `tsc && vite build` fehlerfrei.

- [ ] **Step 3: Live-Smoke-Test des vollständigen Zyklus**

`npm run tauri dev` starten. Eine unwichtige/doppelte Testdatei aus dem eigenen Katalog löschen, prüfen:
1. Datei verschwindet aus der normalen Ansicht.
2. Datei liegt jetzt physisch unter `~/.local/share/com.thebexxs.mfkatalogmanager/trash/` (Pfad per `find ~/.local/share/com.thebexxs.mfkatalogmanager/trash -type f` verifizieren), NICHT mehr am Originalort.
3. Papierkorb-Icon im Header zeigt Badge mit Zahl 1 (oder mehr).
4. Papierkorb-Ansicht öffnen, die gelöschte Datei erscheint dort mit Metadaten + kleiner 3D-Vorschau.
5. "Wiederherstellen" klicken (oder per `sqlite3`/`invoke`-Nachvollzug, falls kein echter Klick möglich ist — dann den Command-Aufrufpfad im Code nochmal genau gegenlesen statt zu raten) — Datei liegt danach wieder am Originalpfad, ist wieder im normalen Katalog sichtbar.

Falls ein echter interaktiver Test nicht durchführbar ist: mindestens Schritt 1-3 per Datei-/DB-Inspektion nach einem über die App tatsächlich ausgelösten Löschvorgang verifizieren (nicht nur Code lesen).

Dev-Server danach beenden.

- [ ] **Step 4: Kein Commit in diesem Task** (reiner Verifikationsschritt; bei gefundenen Problemen zurück zum jeweiligen Task, dort fixen und dort erneut committen).
