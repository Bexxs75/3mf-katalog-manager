<!-- What does this PR do, and why? -->



<!-- Linked issue, if any -->
Closes #

## Checklist

- [ ] Tests run: `npx tsc --noEmit`, `npx vitest run`, and (if backend changed) `cargo test --no-default-features` in `src-tauri`
- [ ] `CHANGELOG.md` `[Unreleased]` section updated (English + German) if this is user-visible
- [ ] New/changed UI text added to all 4 language files (`src/i18n/de.ts`, `en.ts`, `es.ts`, `fr.ts`) if UI text changed
- [ ] Screenshots attached below if this changes the UI
